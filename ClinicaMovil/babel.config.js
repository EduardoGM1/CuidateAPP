module.exports = {
  presets: ['module:@react-native/babel-preset'],
  plugins: [
    // Plugin de reanimated (si está instalado)
    // 'react-native-reanimated/plugin', // Descomentar si se instala reanimated
  ],
};
